params:
  - id: init_admin
    type: string
    name: "Initial Admin Username"
    description: "Username for the initial admin account"

  - id: init_admin_password
    type: secret
    name: "Initial Admin Password"
    description: "Password for the initial admin account"
    default_value: "$random(len=24)"
    optional: false

locales:
  zh:
    init_admin:
      name: "初始管理员用户名"
      description: "初始管理员的用户名, 必须是字母或数字"

    init_admin_password:
      name: "初始管理员密码"
      description: "初始管理员的密码"
